import css from './TransactionHistory.module.css';

export const TransactionHistory = ({ item }) => {
  return (
    <thead>
      <tr>
        {/* <th>{type}</th>
        <th>{amount}</th>
        <th>{currency}</th> */}
      </tr>
    </thead>
  );
};
